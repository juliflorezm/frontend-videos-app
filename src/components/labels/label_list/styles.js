import { makeStyles } from "@material-ui/core";

export const useStyles = makeStyles((theme) => ({
  labelsBar: {
    backgroundColor: "#282c34",
    borderBottom: "rgb(192, 200, 248) 2px solid",
    borderTop: "rgb(192, 200, 248) 2.5px solid",
  },
  margin: {
    margin: "auto",
  },
}));
